fastapi>=0.115.0
uvicorn[standard]>=0.30.0
asyncpg>=0.29.0
httpx>=0.27.0
pydantic>=2.0.0
pyyaml>=6.0
# Phase 35: External World tab's revenue-by-customer chart reads Akaunting's
# MariaDB directly (same "read the ledger directly" pattern Phase 31's Grafana
# MySQLAkaunting datasource and purge-manager/snapshot-manager already use) —
# aiomysql is a pure-Python async driver, no extra native deps needed.
aiomysql>=0.2.0
